config system ntp
    set ntpsync enable
    set type custom
    config ntpserver
        edit 1
            set server "be.pool.ntp.org"
        next
    end
end

config system global
set timezone "Europe/Brussels"
end
